name: 🎉 Positive Vibes Demo
on:
  push:
    branches: [main]

jobs:
  deployment-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check team positive vibes before deploy
        uses: codeanding/positive-vibes-gate@v1.1.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          mode: deployment
          days-lookback: 7
          require-count: 1
      - name: Simulate deployment
        run: echo "🚀 Deploying with good team vibes!"
