name: 🎉 Positive Vibes Demo
on:
  push:
    branches: [main]

jobs:
  deployment-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check last merged PR positive vibes
        uses: codeanding/positive-vibes-gate@v1.2.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          mode: last-merge
      - name: Simulate deployment
        run: echo "🚀 Deploying with good team vibes!"
